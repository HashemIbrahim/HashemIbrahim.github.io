import React from "react";
import TerminalPortfolio from "./TerminalPortfolio";

export default function Portfolio({ innerRef }) {
  return <TerminalPortfolio innerRef={innerRef} sectionId="portfolio" />;
}